{
  "plugins": {
    "@release-it/conventional-changelog": {
      "preset": "angular",
      "infile": "CHANGELOG.md"
    }
  },
  "git": {
    "commit": true,
    "tag": true,
    "push": true,
    "tagName": "v${version}"
  },
  "github": {
    "release": false
  },
  "npm": {
    "publish": false
  },
  "hooks": {
    "before:init": "pnpm -r run typecheck && pnpm -r run test",
    "after:bump": "pnpm -r --filter='./packages/*' exec npm pkg set version=${version}"
  }
}
