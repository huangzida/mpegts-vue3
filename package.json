{
  "name": "mpegts-vue3-monorepo",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -C apps/demo dev",
    "dev:react": "pnpm -C apps/react-demo dev",
    "release": "release-it"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@9.15.4",
  "devDependencies": {
    "@release-it/conventional-changelog": "^11.0.0",
    "release-it": "^20.0.1"
  }
}
